// AiaSoft #913 — მანდარინები და ფორთოხლები
// Complexity: O(log(min(N, M)))
// Topics: Math, Number Theory

#include <iostream>
#include <algorithm> 

using namespace std;

int main(){
    
    int n, m;
    cin>>n>>m;
	cout<<__gcd(n, m);
	
	return 0;
}
